import 'package:flutter/material.dart';

class RegistKendaraan extends StatelessWidget {
  const RegistKendaraan({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
